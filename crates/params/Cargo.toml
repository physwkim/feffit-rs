[package]
name = "params"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "lmfit-style parameters with constraint expressions (asteval subset) for feffit-rs"

[dependencies]
