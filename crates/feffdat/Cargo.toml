[package]
name = "feffdat"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure-Rust port of xraylarch's feffdat: parse feffNNNN.dat and compute the EXAFS chi(k) for a Feff path."

[dependencies]
num-complex = { workspace = true }
