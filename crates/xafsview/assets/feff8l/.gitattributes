# Vendored FEFF8.5L (Feff8L) executables + gfortran runtime, per platform.
# Treat as binary so git never applies line-ending/text normalization.
feff8l_* binary
*.so binary
*.dylib binary
*.dll binary
